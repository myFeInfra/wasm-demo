export function sum(a: i32, b: i32): i32 {
  let res: i32 = 0;
  for (let i = 0; i < 10000000; i++) {
    // 模拟一个性能评估任务实现
    res += 0  
  }
  return a + b;
}

export function sub(a: i32, b: i32): i32 {
  return a - b;
}